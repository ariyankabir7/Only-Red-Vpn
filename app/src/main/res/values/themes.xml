<resources xmlns:tools="http://schemas.android.com/tools">
    <!-- Base application theme. -->
    <style name="Base.Theme.OnlyRedVpn" parent="Theme.Material3.DayNight.NoActionBar">
        <!-- Customize your light theme here. -->
        <!-- <item name="colorPrimary">@color/my_light_primary</item> -->
        <item name="fontFamily">@font/gff_medium</item>
    </style>

    <style name="Theme.OnlyRedVpn" parent="Base.Theme.OnlyRedVpn" />

    <style name="TransparentDialogTheme" parent="Theme.AppCompat.Dialog">
        <item name="android:windowBackground">@android:color/transparent</item>
        <item name="android:windowMinWidthMajor">100%</item>
        <item name="android:windowMinWidthMinor">100%</item>
        <item name="colorAccent">#000000</item>
        <item name="android:backgroundDimEnabled">true</item> <!-- Optional: Dim the background -->
    </style>
</resources>